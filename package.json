{
  "name": "bitget-desk",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "dev:prequel": "pnpm --filter prequel dev",
    "dev:landed": "pnpm --filter landed dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck",
    "probe": "node scripts/mcp-probe.mjs"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "6.0.3",
    "vitest": "^5.0.1"
  }
}
